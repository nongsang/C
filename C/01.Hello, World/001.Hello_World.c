#include <stdio.h>	// stdio.h를 포함하여 사용하고자 하는 함수가 있는 라이브러리를 명시

int main(void)		// main() 함수를 호출하여 실행한다.
{
	printf("hello, world\n");	// 화면에 hello, world라고 출력한다.
	return 0;					// 0을 운영체제에 반환하여 종료를 알린다.
}